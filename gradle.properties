# Done to increase the memory available to Gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
# Mod properties
mod_version=1.1
maven_group=traben
archives_name=entity_pin_cushions
enabled_platforms=fabric,forge
# Minecraft properties
minecraft_version=1.21.11
# Dependencies
fabric_loader_version=0.19.3
neoforge_version=21.11.45
forge_version=1.21.11-61.0.5

# Parchment (readable parameter names) is intentionally NOT enabled by default here.
# At the time of this port the exact release-export version for 1.21.11 could not be
# verified reliably. If you want it, check https://parchmentmc.org/docs/getting-started
# for the current "parchment-1.21.11" export version, set it below, and uncomment the
# parchment(...) line in the root build.gradle.
#parchment_version=1.21.11:YYYY.MM.DD